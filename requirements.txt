# NeuraRoute Python dependencies (engine, runtime, contracts fakes)
paho-mqtt>=2.1        # MQTT client (engine + device agent + fakes)
fastapi>=0.110        # engine HTTP + WebSocket
uvicorn[standard]>=0.29
python-multipart>=0.0.9  # PDF file upload on POST /request
websockets>=12.0      # fake_engine WS server for dashboard dev
jsonschema>=4.21      # validate messages against contracts/*.schema.json
pyyaml>=6.0           # runtime device configs + engine policy.yaml
requests>=2.31        # models/ triage — LLM + cloud adapters (OpenAI-compatible HTTP)
pyserial>=3.5         # runtime arduino watchdog -> STM32 serial bridge (optional; log-only if absent)
# --- inference module: the standalone /infer servers (servers/) ---
groq>=0.11            # servers/cloud_server.py — Groq/Llama-3.3-70B cloud tier
python-dotenv>=1.0    # servers/cloud_server.py — load GROQ_API_KEY from .env
