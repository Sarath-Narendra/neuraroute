# Tier 1 of the ladder: GPT via the cloud adapter. "Internet down" = this tier's
# GPT call errors (or the agent goes stale) -> the engine slides down the ladder.
device_id: cloud-01
device_type: cloud
supported_ops:
  - echo
  - triage
privacy_ok: false
